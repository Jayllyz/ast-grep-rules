id: no-primitive-jpa-id
valid:
  - |
    @Entity
    class Order {
        @Id
        private Long id;
    }
  - |
    @Entity
    class Order {
        @Id
        private Integer id;
    }
  - |
    @Entity
    class Order {
        private long count;
    }
  - |
    @Entity
    class Order {
        @Id
        private String id;
    }
invalid:
  - |
    @Entity
    class Order {
        @Id
        private long id;
    }
  - |
    @Entity
    class Order {
        @Id
        private int id;
    }
  - |
    @Entity
    class Order {
        @Id
        private short id;
    }
  - |
    @Entity
    class Order {
        @Id
        @GeneratedValue(strategy = GenerationType.IDENTITY)
        private long id;
    }
